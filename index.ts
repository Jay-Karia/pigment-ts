import { convertColor } from "./utils/convertColor";
import { lightenColor, darkenColor } from "./utils/lightenDarken";
import { blendColors } from "./utils/blendColors";
import { setOpacity } from "./utils/opacity";
import { randomColor } from "./utils/randomColor";

export {
  convertColor,
  lightenColor,
  darkenColor,
  blendColors,
  setOpacity,
  randomColor,
};

const PigmentJS = {
  convertColor,
  lightenColor,
  darkenColor,
  blendColors,
  setOpacity,
  randomColor,
};

export default PigmentJS;
